on: push

defaults:
  run:
    shell: bash

jobs:
  rando-title:
    runs-on: debian
    steps: 
      - uses: actions/checkout@v2
      - run: bash
      - run: sudo apt install hugo
      - run: echo 'Hugo has been installed!'
      - run: ls
      - run: hugo  
      - run: echo 'Files have been compiled into a working site'
